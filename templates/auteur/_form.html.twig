<div class="form-ajout">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-adds btn-up">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
</div>

